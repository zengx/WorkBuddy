# 2026-07-23 工作记录

## 创建 wechat-push skill（Server酱微信推送）
- 路径（用户级）：`~/.workbuddy/skills/wechat-push/`
- 组成：`SKILL.md`、`scripts/push.py`、`references/serverchan.md`
- 能力：通过 Server酱（ServerChan）把消息推送到个人微信；支持手动即时推送 + 配合 WorkBuddy 自动化定时推送。
- SendKey 来源优先级：`--sendkey` > 环境变量 `SERVERCHAN_SENDKEY` > `config.json`；Turbo 版(SCT)与旧版(SCU)按前缀自动选接口。
- 已用 skill-creator 的 init/package 脚本初始化并校验通过，打包产物：`/Users/zengx/WorkBuddy/2026-07-23-12-00-28/wechat-push.zip`
- 测试：无 Key 返回 -2、无效 Key 优雅返回 HTTP 400 JSON、--set-key 正确写入 config.json（测试后已清理）。
- **已配置真实 SendKey 到 `config.json`（Turbo 版），并发起真实测试推送，返回 code:0/SUCCESS（pushid:47042521），确认可推到用户微信。**
- 注意：未处理 BOOTSTRAP.md 身份初始化（用户直接给了具体任务，留待后续）。
